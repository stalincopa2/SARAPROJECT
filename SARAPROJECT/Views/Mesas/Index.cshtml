@model IEnumerable<SARAPROJECT.Models.Mesa>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.CodMesa)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.NombreMesa)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdSalonNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CodMesa)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NombreMesa)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdSalonNavigation.IdSalon)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdMesa">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdMesa">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdMesa">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
